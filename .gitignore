*.egg-info/
build/

*.pyc
__pycache__/
.idea
venv
dist

# produced by `pip install -e .`
exllamav3_ext.cpython-*.so
# training run outputs (adapters/checkpoints/logs)
out/
# qlora_runs.csv is TRACKED as of 2026-07-14 (history protects against the
# .bak-rotation clobber); sidecars and rotation backups stay untracked
qlora_runs.csv.errors.log
qlora_runs.csv.*
# unsloth writes its compiled kernel cache into cwd
unsloth_compiled_cache/
